using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class SferasKontrole : MonoBehaviour
{/*
    public GameObject Capsule;
    public GameObject Text;
    //  OnTriggerEnter metodē pārbaudām, vai sadursmes objekta tags ir “Kapsula” un ja nosacījums izpildās, izmainīt izveidotā Text objekta tekstu uz “Tu uzvarēji!”

    private void OnTriggerEnter(Collider other)
    {
        if (other.gameObject.tag == "Kapsula")
        {
            GetComponent<Text>().text = "Tu uzvarēji!";
        }
    }
    */
}
